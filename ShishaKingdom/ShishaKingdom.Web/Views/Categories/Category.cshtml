@using ShishaKingdom.Web.ViewModels.Products
@model IEnumerable<ShishaKingdom.Web.ViewModels.Products.ProductViewModel>

@{
    ViewBag.Title = "Категория" + @ViewBag.CategoryName;
}



<link href="@Url.Content("~/Content/productsStyle.css")" rel="stylesheet" type="text/css" />



<div class="row">
    <h2>@ViewBag.CategoryName</h2>
    <div class="btn-group" style="margin-bottom: 5px">
        <div class="col-lg-2">
            <form method="get" action="category">
                <div class="input-group">
                    <input type="text" class="form-control" placeholder="Име на продукт..." name="search" id="search">
                    <input type="hidden" value="@ViewBag.CategoryId" name="id" />
                    <span class="input-group-btn">
                        <button class="btn btn-default" type="submit">Търси</button>
                    </span>
                </div>
            </form>
        </div>
        @if (User.IsInRole("Admin"))
        {
            <a href="/administration/products/addproduct" class="btn btn-success" role="button" id="newProductBtn">Нов Продукт</a>
        }

    </div>
    <div class="col-md-12">
        @Html.Action("Products", "Products", new { categoryId = @ViewBag.CategoryId, search = ViewBag.Search })
    </div>
</div>
